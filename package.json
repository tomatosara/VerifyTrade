{
  "name": "verifytrade",
  "private": true,
  "packageManager": "pnpm@9.6.0",
  "scripts": {
  "build": "turbo build",
  "dev": "turbo run dev --parallel",
  "dev:2": "turbo run dev --parallel --filter=backend --filter=frontend",
  "lint": "turbo lint",
  "test": "turbo test",
  "typecheck": "turbo typecheck"
  },
  "devDependencies": {
  "turbo": "^2.1.0"
  }
}