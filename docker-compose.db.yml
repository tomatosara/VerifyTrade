services:
  db:
    image: postgres:16-alpine
    container_name: verifytrade-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-app_db}
      POSTGRES_USER: ${POSTGRES_USER:-app}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-app}
    ports:
      # Override HOST_DB_PORT=5433 (or another open port) if 5432 is already in use.
      - "${HOST_DB_PORT:-5432}:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-app} -d ${POSTGRES_DB:-app_db}"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 5s
    networks:
      - backend_net

  # pgadmin:
  #   image: dpage/pgadmin4:8.5
  #   container_name: verifytrade-pgadmin
  #   restart: unless-stopped
  #   environment:
  #     PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT_EMAIL:-admin@example.com}
  #     PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_DEFAULT_PASSWORD:-admin}
  #   ports:
  #     - "5050:5050"
  #   depends_on:
  #     db:
  #       condition: service_healthy
  #   networks:
  #     - backend_net

volumes:
  db_data:

networks:
  backend_net:
    driver: bridge
